{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "build": "tsc -p tsconfig.build.json",
    "deploy": "sed -i '\\/\\/\\/ ESBUILD_DEPLOY_STOP/a/*' index.ts && esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@aws-sdk/* --tsconfig=tsconfig.build.json --define:process.env.NODE_ENV='\"production\"' && sed -i '/\\/\\*/d' index.ts  && cd ../infra && cdk deploy --hotswap",
    "start": "bun run index.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.859.0",
    "@aws-sdk/lib-dynamodb": "^3.859.0",
    "@trpc/server": "^11.4.3",
    "aws-jwt-verify": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "shared": "workspace:*",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "typescript": "^5.0.0"
  }
}
